{% if modules %}
    
    {% for module in modules.modules %}
        {%  set module_name = module.acf_fc_layout|replace({'.': '/'})~'.twig' %}
        {% include module_name with {"acf": module} %}
    {% endfor %}
{% endif %}

{# @if ( $modules )
    @foreach ($modules['modules'] as $module)
        @php $layout = $module["acf_fc_layout"]; @endphp
        {{-- @dump($module) --}}
        @include($layout, ['acf' => $module])
    @endforeach
@endif #}
